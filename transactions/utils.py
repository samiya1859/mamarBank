# from .models import Transaction

# def is_bankrupt():
#     total_balance = Transaction.objects.aggregate(total_balance=models.Sum('amount'))['total_balance']
#     bankruptcy_threshold = 0
#     return total_balance < bankruptcy_threshold
